interface:
  display_name: "Jevals"
  short_description: "Create, run, and inspect reviewed Jev evaluations"
  default_prompt: "Use $jevals to work with evaluations in the local Jevals workbench."
policy:
  allow_implicit_invocation: true
